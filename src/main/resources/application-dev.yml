spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parking
    username: parking
    password: parking
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  ml:
    simulate-sensors: true
